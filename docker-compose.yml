services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: recipe_service
      POSTGRES_USER: recipe_service
      POSTGRES_PASSWORD: recipe_service
    ports:
      - "5432:5432"
    volumes:
      - recipe-service-postgres-data:/var/lib/postgresql/data

volumes:
  recipe-service-postgres-data:
